# Traefik bootstrap for Homerun's deployed services.
#
# This app itself is NOT containerized yet — run it directly on the host
# (`bun run dev` / `bun run start`) with DOCKER_NETWORK_NAME=homerun-network
# and access to /var/run/docker.sock. Traefik auto-discovers containers this
# app creates via Docker labels (see src/lib/server/docker/labels.ts) — no
# control-plane push needed.
#
# No-compose fallback: `docker network create homerun-network`, then run
# Traefik standalone with the same command flags as below.
services:
  traefik:
    image: traefik:v3
    restart: unless-stopped
    command:
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.network=homerun-network
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.letsencrypt.acme.httpchallenge=true
      - --certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web
      - --certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL:-admin@example.com}
      - --certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json
      # Opt-in: only needed for custom SSL certificates (see the service
      # Networking tab and $lib/server/docker/custom-ssl.ts). Uncomment
      # this line, set TRAEFIK_DYNAMIC_CONFIG_DIR to the same host path
      # as the bind mount below, and `docker compose up -d` to pick it
      # up — the app writes cert/key files + per-domain dynamic config
      # into that directory itself, it does not touch this container.
      # - --providers.file.directory=/etc/traefik/dynamic
      # - --providers.file.watch=true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik-certs:/letsencrypt
      # Uncomment alongside the --providers.file.* flags above — must be
      # the same host path this app's TRAEFIK_DYNAMIC_CONFIG_DIR points at.
      # - ${TRAEFIK_DYNAMIC_CONFIG_DIR:-./traefik-dynamic}:/etc/traefik/dynamic:ro
    networks:
      - homerun-network

networks:
  # external: true — this network is a shared resource, not compose-owned.
  # The app's own Docker layer (src/lib/server/docker/service.ts) attaches
  # deployed containers to it directly by name, and it must survive a
  # `docker compose down` without taking those containers' routing with it.
  # Create it once with: docker network create homerun-network
  homerun-network:
    name: homerun-network
    external: true

volumes:
  traefik-certs: {}
