node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
# packages/docs-site/ is its own separate SvelteKit build (see package.json's
# docs-site:* scripts), same output dir names, different subtree.
packages/docs-site/.svelte-kit
packages/docs-site/build
# Compiled binaries from agent/, installer/, cli/ (bun build --compile)
dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
tmp/
*.db
test-results/
coverage/

/tmp/
