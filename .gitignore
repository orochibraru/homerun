node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
# packages/docs/ is its own separate SvelteKit build (see package.json's
# *:docs scripts), same output dir names, different subtree.
packages/docs/.svelte-kit
packages/docs/build
# Copied in at dev/build time from the repo root's own openapi.json
# (scripts/docs.ts), not committed, see packages/docs/README.md.
packages/docs/static/openapi.json
# Compiled binaries from agent/, installer/, cli/ (bun build --compile)
dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Config
homerun.yaml

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
tmp/
*.db
test-results/
coverage/

/tmp/
