services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: app
    extends:
      file: tools/compose/app.compose.yaml
      service: app
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
      - homerun

  worker:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: app
    extends:
      file: tools/compose/worker.compose.yaml
      service: worker
    environment:
      AUTH_SECRET: ${AUTH_SECRET:?set AUTH_SECRET in .env before starting, e.g. openssl rand -hex 32 or bunx auth secret}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
      - homerun

  agent:
    build:
      context: .
      dockerfile: ./Dockerfile
      target: agent
    extends:
      file: tools/compose/agent.compose.yaml
      service: agent
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
      - homerun

  traefik:
    extends:
      file: tools/compose/base.compose.yaml
      service: traefik
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal
      - homerun

  postgres:
    extends:
      file: tools/compose/base.compose.yaml
      service: postgres
    networks:
      - internal

networks:
  homerun:
    name: homerun
    external: true
  internal:
    external: false

volumes:
  postgres_data: {}
  traefik_certs: {}
  traefik_dynamic: {}
  agent_data: {}
  app_data: {}
