# TODO

The backlog, and the only one. There is no priority ordering, pick whatever.
When done delete the entry, no bloat.

- [ ] [App] Dokploy compose migration keeps plain named-volume names, Dokploy
      prefixes them with the stack's `appName`, so migrated stacks on the same
      host don't reuse the existing data.
- [ ] [Tooling] **Homerun SDK**, a shared library with the CLI.
- [ ] [SDKs] **Terraform and Pulumi providers.**
- [ ] [Docker] **Image security scanning.**
