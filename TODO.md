# TODO

The backlog, and the only one. There is no priority ordering, pick whatever.
When done delete the entry, no bloat.

- [ ] [Docker] Garbage-collect the `homerun-mirror` registry, it keeps every
      image version ever deployed.
- [ ] [API] Expose image scan results in the REST API/OpenAPI and the CLI.
