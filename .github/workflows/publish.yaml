name: Publish
on:
  push:
    branches:
      - main
    paths-ignore:
      - "docs/**"
      - "**/*.md"
      - ".agents/**"
      - ".claude/**"

concurrency:
  group: publish
  cancel-in-progress: false

env:
  TZ: Europe/Paris
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CI: true

permissions:
  contents: read
  packages: write

jobs:
  resolve:
    name: Resolve Merged PR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      checks: read
    outputs:
      pr: ${{ steps.resolve.outputs.pr }}
      promote: ${{ steps.resolve.outputs.promote }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Find the PR and whether its images can be promoted
        id: resolve
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          echo "promote=false" >> "$GITHUB_OUTPUT"

          pr="$(gh api "repos/$REPO/commits/$SHA/pulls" --jq '[.[] | select(.merged_at != null)][0].number // empty')"
          echo "pr=$pr" >> "$GITHUB_OUTPUT"
          if [ -z "$pr" ]; then
            echo "Direct push, no PR to promote from: building from scratch."
            exit 0
          fi

          head="$(gh api "repos/$REPO/pulls/$pr" --jq '.head.sha')"
          git fetch --quiet origin "refs/pull/$pr/head"
          drift="$(git diff --name-only "$head" "$SHA" -- . ':(exclude)CHANGELOG.md' ':(exclude)package.json')"
          if [ -n "$drift" ] || [ "$(git show "$head:package.json" | jq -S 'del(.version)')" != "$(git show "$SHA:package.json" | jq -S 'del(.version)')" ]; then
            echo "PR #$pr differs from this commit beyond a release bump, so its images don't match: building from scratch."
            echo "$drift"
            exit 0
          fi

          gate="$(gh api "repos/$REPO/commits/$head/check-runs?check_name=CI%20Gate" --jq '[.check_runs[] | select(.conclusion == "success")] | length')"
          if [ "$gate" = "0" ]; then
            echo "CI Gate never passed on PR #$pr's head $head: building from scratch."
            exit 0
          fi

          for image in homerun homerun-agent; do
            if ! docker buildx imagetools inspect "docker.io/orochibraru/$image:pr-$pr" > /dev/null 2>&1; then
              echo "docker.io/orochibraru/$image:pr-$pr doesn't exist: building from scratch."
              exit 0
            fi
          done

          echo "Promoting PR #$pr's already-tested images instead of rebuilding."
          echo "promote=true" >> "$GITHUB_OUTPUT"

  code_quality:
    name: Code Quality
    needs: [resolve]
    if: needs.resolve.outputs.promote != 'true'
    uses: ./.github/workflows/code_quality.yaml
    secrets: inherit

  version:
    name: Compute Release Version
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.compute.outputs.tag }}
      version: ${{ steps.compute.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN }}

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json

      - name: Setup Node.js for semantic-release
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Dry-run release to compute the next version
        id: compute
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          if ! output="$(bun run semantic-release --dry-run 2>&1)"; then
            echo "$output"
            echo "::error::semantic-release dry run failed, refusing to build images stamped with the wrong version"
            exit 1
          fi
          echo "$output"
          version="$(echo "$output" | grep -oE 'release version is [0-9]+\.[0-9]+\.[0-9]+' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | tail -n1)"
          if [ -n "$version" ]; then
            echo "This push will release v$version, tagging images and binaries accordingly."
            echo "tag=v$version" >> "$GITHUB_OUTPUT"
            echo "version=$version" >> "$GITHUB_OUTPUT"
          else
            echo "No release-worthy commits, falling back to the commit SHA for image tags; binaries keep the last released version."
            echo "tag=${{ github.sha }}" >> "$GITHUB_OUTPUT"
            echo "version=" >> "$GITHUB_OUTPUT"
          fi

  build-app:
    name: Docker Build (App)
    uses: ./.github/workflows/docker.yaml
    needs:
      - code_quality
      - version
    with:
      app_version: ${{ needs.version.outputs.version }}
      image_tag: ${{ needs.version.outputs.tag }}
      image_name: orochibraru/homerun
      registry_name: docker.io
      registry_username: orochibraru
      bake_target: app-ci
    secrets:
      registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  build-agent:
    name: Docker Build (Agent)
    uses: ./.github/workflows/docker.yaml
    needs:
      - code_quality
      - version
    with:
      image_tag: ${{ needs.version.outputs.tag }}
      image_name: orochibraru/homerun-agent
      registry_name: docker.io
      registry_username: orochibraru
      bake_target: agent-ci
    secrets:
      registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  binaries:
    name: Binaries
    uses: ./.github/workflows/binaries.yaml
    needs:
      - code_quality
      - version
    if: ${{ !cancelled() && !contains(needs.*.result, 'failure') }}
    with:
      version: ${{ needs.version.outputs.version }}

  e2e:
    name: E2E
    needs: [build-app]
    uses: ./.github/workflows/e2e.yaml
    with:
      image: ${{ needs.build-app.outputs.image }}
      registry_name: docker.io
      registry_username: orochibraru
      bake_target: app-ci
    secrets:
      registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  publish-app:
    name: Publish App ${{ needs.version.outputs.tag }}
    needs: [version, e2e]
    uses: ./.github/workflows/docker-manifest.yaml
    with:
      image_name: orochibraru/homerun
      registry_name: docker.io
      registry_username: orochibraru
      bake_target: app-ci
    secrets:
      registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  publish-agent:
    name: Publish Agent ${{ needs.version.outputs.tag }}
    needs: [version, e2e, build-agent]
    uses: ./.github/workflows/docker-manifest.yaml
    with:
      image_name: orochibraru/homerun-agent
      registry_name: docker.io
      registry_username: orochibraru
      bake_target: agent-ci
    secrets:
      registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  promote:
    name:
      Promote pr-${{ needs.resolve.outputs.pr }} to ${{
      needs.version.outputs.tag }}
    needs: [resolve, version]
    if: needs.resolve.outputs.promote == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Login to Container registry
        uses: docker/login-action@v4
        with:
          registry: docker.io
          username: orochibraru
          password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Tag the tested images
        env:
          PR: ${{ needs.resolve.outputs.pr }}
          TAG: ${{ needs.version.outputs.tag }}
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          set -euo pipefail
          app=docker.io/orochibraru/homerun
          printf 'FROM %s:pr-%s\nENV HOMERUN_APP_VERSION=%s\n' "$app" "$PR" "$VERSION" \
            | docker buildx build --platform linux/amd64,linux/arm64 -t "$app:$TAG" -t "$app:latest" --push -
          docker buildx imagetools inspect "$app:$TAG"
          agent=docker.io/orochibraru/homerun-agent
          docker buildx imagetools create -t "$agent:$TAG" -t "$agent:latest" "$agent:pr-$PR"
          docker buildx imagetools inspect "$agent:$TAG"

  cleanup:
    name: PR Cleanup
    needs: [resolve, promote, publish-app, publish-agent]
    if:
      ${{ !cancelled() && needs.resolve.outputs.pr != '' &&
      !contains(needs.*.result, 'failure') }}
    uses: ./.github/workflows/delete-pr-images.yaml
    with:
      pr_number: ${{ needs.resolve.outputs.pr }}
    secrets:
      registry_password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}

  release:
    name: Release
    runs-on: ubuntu-latest
    needs:
      - code_quality
      - version
      - build-app
      - build-agent
      - binaries
      - e2e
      - publish-app
      - publish-agent
      - promote
    if:
      ${{ !cancelled() && !contains(needs.*.result, 'failure') &&
      (needs.promote.result == 'success' || (needs.publish-app.result ==
      'success' && needs.publish-agent.result == 'success')) }}
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      # RELEASE_TOKEN (a fine-grained PAT scoped to just this repo, Contents +
      # Issues + Pull requests: write) is required, not the default
      # GITHUB_TOKEN: the `main` ruleset blocks direct pushes from anyone but
      # a repo admin, and the default token's actor (github-actions[bot]) isn't
      # on the bypass list. Used both for git's push auth here and as GH_TOKEN
      # below for @semantic-release/github's API calls.
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN }}

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: package.json

      - name: Setup Node.js for semantic-release
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      - name: Download binaries
        uses: orochibraru/download-artifact@v8
        with:
          path: dist
          pattern: binaries-*
          merge-multiple: true

      - name: Release
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: bun run release

      - name: Upload binaries and publish the release
        if: ${{ hashFiles('.release-tag') != '' }}
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: bun scripts/upload-release-assets.ts "$(cat .release-tag)"
